# Simple Browser Notes App

This project is a beginner-friendly notes app that runs directly in the browser.

## How to run

1. Open `index.html` in any browser.
2. Add notes, mark notes as done, and delete notes.

## Learning focus

- Arrays (`notes`)
- Loops (`for...of` and indexed `for`)
- Basic DOM updates (`renderNotes`)